from __future__ import annotations
import sys
from importlib.machinery import ModuleSpec, PathFinder
from importlib.machinery import all_suffixes as module_suffixes
from importlib.util import spec_from_file_location
from itertools import chain
from pathlib import Path

MAPPING: dict[str, str] = {'acp_adapter': '/home/opc/.hermes/hermes-agent/acp_adapter', 'agent': '/home/opc/.hermes/hermes-agent/agent', 'batch_runner': '/home/opc/.hermes/hermes-agent/batch_runner', 'cli': '/home/opc/.hermes/hermes-agent/cli', 'cron': '/home/opc/.hermes/hermes-agent/cron', 'gateway': '/home/opc/.hermes/hermes-agent/gateway', 'hermes_bootstrap': '/home/opc/.hermes/hermes-agent/hermes_bootstrap', 'hermes_cli': '/home/opc/.hermes/hermes-agent/hermes_cli', 'hermes_constants': '/home/opc/.hermes/hermes-agent/hermes_constants', 'hermes_logging': '/home/opc/.hermes/hermes-agent/hermes_logging', 'hermes_state': '/home/opc/.hermes/hermes-agent/hermes_state', 'hermes_time': '/home/opc/.hermes/hermes-agent/hermes_time', 'mcp_serve': '/home/opc/.hermes/hermes-agent/mcp_serve', 'model_tools': '/home/opc/.hermes/hermes-agent/model_tools', 'plugins': '/home/opc/.hermes/hermes-agent/plugins', 'providers': '/home/opc/.hermes/hermes-agent/providers', 'run_agent': '/home/opc/.hermes/hermes-agent/run_agent', 'tools': '/home/opc/.hermes/hermes-agent/tools', 'toolset_distributions': '/home/opc/.hermes/hermes-agent/toolset_distributions', 'toolsets': '/home/opc/.hermes/hermes-agent/toolsets', 'trajectory_compressor': '/home/opc/.hermes/hermes-agent/trajectory_compressor', 'tui_gateway': '/home/opc/.hermes/hermes-agent/tui_gateway', 'utils': '/home/opc/.hermes/hermes-agent/utils'}
NAMESPACES: dict[str, list[str]] = {'gateway.assets': ['/home/opc/.hermes/hermes-agent/gateway/assets'], 'plugins.browser': ['/home/opc/.hermes/hermes-agent/plugins/browser'], 'plugins.dashboard_auth': ['/home/opc/.hermes/hermes-agent/plugins/dashboard_auth'], 'plugins.example-dashboard': ['/home/opc/.hermes/hermes-agent/plugins/example-dashboard'], 'plugins.hermes-achievements': ['/home/opc/.hermes/hermes-agent/plugins/hermes-achievements'], 'plugins.image_gen': ['/home/opc/.hermes/hermes-agent/plugins/image_gen'], 'plugins.kanban': ['/home/opc/.hermes/hermes-agent/plugins/kanban'], 'plugins.model-providers': ['/home/opc/.hermes/hermes-agent/plugins/model-providers'], 'plugins.observability': ['/home/opc/.hermes/hermes-agent/plugins/observability'], 'plugins.platforms': ['/home/opc/.hermes/hermes-agent/plugins/platforms'], 'plugins.video_gen': ['/home/opc/.hermes/hermes-agent/plugins/video_gen'], 'plugins.example-dashboard.dashboard': ['/home/opc/.hermes/hermes-agent/plugins/example-dashboard/dashboard'], 'plugins.hermes-achievements.dashboard': ['/home/opc/.hermes/hermes-agent/plugins/hermes-achievements/dashboard'], 'plugins.hermes-achievements.docs': ['/home/opc/.hermes/hermes-agent/plugins/hermes-achievements/docs'], 'plugins.hermes-achievements.tests': ['/home/opc/.hermes/hermes-agent/plugins/hermes-achievements/tests'], 'plugins.hermes-achievements.dashboard.dist': ['/home/opc/.hermes/hermes-agent/plugins/hermes-achievements/dashboard/dist'], 'plugins.hermes-achievements.docs.assets': ['/home/opc/.hermes/hermes-agent/plugins/hermes-achievements/docs/assets'], 'plugins.kanban.dashboard': ['/home/opc/.hermes/hermes-agent/plugins/kanban/dashboard'], 'plugins.kanban.systemd': ['/home/opc/.hermes/hermes-agent/plugins/kanban/systemd'], 'plugins.kanban.dashboard.dist': ['/home/opc/.hermes/hermes-agent/plugins/kanban/dashboard/dist'], 'tools.neutts_samples': ['/home/opc/.hermes/hermes-agent/tools/neutts_samples']}
PATH_PLACEHOLDER = '__editable__.hermes_agent-0.15.1.finder' + ".__path_hook__"


class _EditableFinder:  # MetaPathFinder
    @classmethod
    def find_spec(cls, fullname: str, path=None, target=None) -> ModuleSpec | None:  # type: ignore
        # Top-level packages and modules (we know these exist in the FS)
        if fullname in MAPPING:
            pkg_path = MAPPING[fullname]
            return cls._find_spec(fullname, Path(pkg_path))

        # Handle immediate children modules (required for namespaces to work)
        # To avoid problems with case sensitivity in the file system we delegate
        # to the importlib.machinery implementation.
        parent, _, child = fullname.rpartition(".")
        if parent and parent in MAPPING:
            return PathFinder.find_spec(fullname, path=[MAPPING[parent]])

        # Other levels of nesting should be handled automatically by importlib
        # using the parent path.
        return None

    @classmethod
    def _find_spec(cls, fullname: str, candidate_path: Path) -> ModuleSpec | None:
        init = candidate_path / "__init__.py"
        candidates = (candidate_path.with_suffix(x) for x in module_suffixes())
        for candidate in chain([init], candidates):
            if candidate.exists():
                return spec_from_file_location(fullname, candidate)
        return None


class _EditableNamespaceFinder:  # PathEntryFinder
    @classmethod
    def _path_hook(cls, path) -> type[_EditableNamespaceFinder]:
        if path == PATH_PLACEHOLDER:
            return cls
        raise ImportError

    @classmethod
    def _paths(cls, fullname: str) -> list[str]:
        paths = NAMESPACES[fullname]
        if not paths and fullname in MAPPING:
            paths = [MAPPING[fullname]]
        # Always add placeholder, for 2 reasons:
        # 1. __path__ cannot be empty for the spec to be considered namespace.
        # 2. In the case of nested namespaces, we need to force
        #    import machinery to query _EditableNamespaceFinder again.
        return [*paths, PATH_PLACEHOLDER]

    @classmethod
    def find_spec(cls, fullname: str, target=None) -> ModuleSpec | None:  # type: ignore
        if fullname in NAMESPACES:
            spec = ModuleSpec(fullname, None, is_package=True)
            spec.submodule_search_locations = cls._paths(fullname)
            return spec
        return None

    @classmethod
    def find_module(cls, _fullname) -> None:
        return None


def install():
    if not any(finder == _EditableFinder for finder in sys.meta_path):
        sys.meta_path.append(_EditableFinder)

    if not NAMESPACES:
        return

    if not any(hook == _EditableNamespaceFinder._path_hook for hook in sys.path_hooks):
        # PathEntryFinder is needed to create NamespaceSpec without private APIS
        sys.path_hooks.append(_EditableNamespaceFinder._path_hook)
    if PATH_PLACEHOLDER not in sys.path:
        sys.path.append(PATH_PLACEHOLDER)  # Used just to trigger the path hook
