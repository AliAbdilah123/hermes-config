User prefers that after the assistant implements a feature or fix, the final response ends with the project's public link.
§
User prefers product/application ideation responses to be delivered as PRD-style HTML artifacts rather than plain text when they ask for app/project concepts.
§
User wants every PRD HTML artifact stored under the relevant project path at docs/<name> and made public at <publicip>/prd/<name> via a symlink from the web server www directory, including current and future PRDs.
§
User wants future PRDs to include a product design system section and to specify shadcn/ui for frontend components in the tech stack.
§
User wants any document produced for them to review to also be provided as a styled HTML file with a public accessible link.
§
User wants every final update after working on a feature or fix to include the project's public link.
§
User prefers every new task to start with a planning phase and a styled HTML review artifact before implementation.
§
User prefers clean code practices when designing or writing code: DRY, meaningful comments for non-obvious logic, and keeping files reasonably sized.
§
User prefers using kanban/task tracking when working on big or complicated features and bugs.