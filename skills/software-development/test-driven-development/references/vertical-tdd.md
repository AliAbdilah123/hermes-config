Vertical TDD guidance (canonical workflow from the `tdd` umbrella)

- Prefer one vertical slice at a time over horizontal batches.
- One behavior per cycle: one failing test, one minimal implementation, one refactor pass.
- Use `tdd` for the primary TDD workflow; this file captures the vertical-slice discipline.